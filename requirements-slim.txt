# Core FastAPI and server dependencies
fastapi>=0.68.0
uvicorn>=0.15.0
gunicorn>=20.1.0
python-multipart>=0.0.5
python-dotenv>=0.19.0

# Database
SQLAlchemy>=1.4.0
psycopg2-binary>=2.9.0

# Authentication
google-auth==2.23.3
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1

# Basic AI/ML (lighter alternatives)
anthropic>=0.3.0
openai==1.61.1

# PDF Processing (lightweight)
pypdf==5.3.0

# Image Processing (essential only)
pillow==11.1.0

# Basic utilities
pydantic>=1.8.0
requests==2.32.3
python-dateutil==2.9.0.post0
tenacity==9.0.0

# OCR - Essential for image processing (using EasyOCR)
easyocr>=1.7.0

# Pinecone (if vector search is essential)
# pinecone==5.4.2  # Comment out if not essential

# Remove heavy ML dependencies that might not be essential:
# - All llama-index packages (very heavy)
# - PyMuPDF (heavy PDF library)
# - NLTK (natural language processing)
# - Pandas (data analysis - only if needed)
# - Boto3 (AWS SDK - only if using AWS)

# If you need any of the commented dependencies, uncomment them selectively